# 🏗️ ILocker System Architecture

## 📊 High-Level Architecture

```
┌────────────────────────────────────────────────────────┐
│                    USER INTERFACE                       │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │ Set Password │ │    Login     │ │     Home     │  │
│  │    Screen    │ │    Screen    │ │    Screen    │  │
│  └──────────────┘ └──────────────┘ └──────────────┘  │
│         │                 │                 │          │
│         └─────────────────┴─────────────────┘          │
│                         │                               │
└─────────────────────────┼───────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────┐
│                 SECURITY SERVICE LAYER                  │
│  ┌────────────────────────────────────────────────┐   │
│  │           SecurityService.ts                    │   │
│  │  • Password Management (PBKDF2)                │   │
│  │  • Master Key Management                       │   │
│  │  • Biometric Authentication                    │   │
│  │  • Auto-Lock & Session Management             │   │
│  │  • Encryption/Decryption (AES-256-GCM)        │   │
│  │  • Key Derivation (HMAC-SHA256)               │   │
│  └────────────────────────────────────────────────┘   │
│                         │                               │
└─────────────────────────┼───────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────┐
│                FILE SERVICE LAYER                       │
│  ┌────────────────────────────────────────────────┐   │
│  │              FileService.ts                     │   │
│  │  • File Picking (Documents/Images/Camera)      │   │
│  │  • File Encryption & Storage                   │   │
│  │  • File Decryption & Reading                   │   │
│  │  • File Deletion                               │   │
│  │  • Metadata Management                         │   │
│  └────────────────────────────────────────────────┘   │
│                         │                               │
└─────────────────────────┼───────────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────────┐
│                  STORAGE LAYER                          │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │   Keychain   │ │     MMKV     │ │  File System │  │
│  │  (Passwords  │ │  (Metadata)  │ │  (Encrypted  │  │
│  │  & Keys)     │ │              │ │   Files)     │  │
│  └──────────────┘ └──────────────┘ └──────────────┘  │
│         │                 │                 │          │
└─────────┼─────────────────┼─────────────────┼──────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌────────────────────────────────────────────────────────┐
│              HARDWARE/OS LAYER                          │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │    Secure    │ │   Android    │ │ App Sandbox  │  │
│  │   Enclave    │ │   Keystore   │ │   Storage    │  │
│  │    (iOS)     │ │  (Android)   │ │              │  │
│  └──────────────┘ └──────────────┘ └──────────────┘  │
└────────────────────────────────────────────────────────┘
```

## 🔐 Security Flow Diagram

```
┌─────────────┐
│    USER     │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────┐
│  1. Enter Password           │
│     (First time or Login)    │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  2. PBKDF2 Hash              │
│     • 10,000 iterations      │
│     • Salt-based             │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  3. Store/Verify in Keychain │
│     • Secure Enclave (iOS)   │
│     • Keystore (Android)     │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  4. Generate Master Key      │
│     • PBKDF2(password,       │
│       salt, 100K iterations) │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  5. Store Master Key         │
│     • Hardware-backed        │
│     • Biometric-protected    │
└──────────────────────────────┘
```

## 📁 File Encryption Flow

```
┌─────────────┐
│  User Picks │
│    File     │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────┐
│  1. Read File Content        │
│     • Convert to Base64      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  2. Generate File ID         │
│     • Timestamp + Random     │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  3. Derive File Key          │
│     • HMAC(masterKey, fileId)│
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  4. Encrypt Content          │
│     • AES-256-GCM            │
│     • Authenticated          │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  5. Save to App Sandbox      │
│     • {fileId}.enc           │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  6. Store Metadata           │
│     • Encrypted MMKV         │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  7. Delete Original File     │
│     • Security measure       │
└──────────────────────────────┘
```

## 🔓 File Decryption Flow

```
┌─────────────┐
│  User Taps  │
│    File     │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────┐
│  1. Verify Authentication    │
│     • Check master key       │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  2. Get File Metadata        │
│     • From MMKV storage      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  3. Derive File Key          │
│     • HMAC(masterKey, fileId)│
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  4. Read Encrypted File      │
│     • From app sandbox       │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  5. Decrypt Content          │
│     • AES-256-GCM            │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  6. Display/Preview          │
│     • In-memory only         │
│     • Never cache            │
└──────────────────────────────┘
```

## 🔄 Authentication State Machine

```
          ┌───────────────────┐
          │   App Launched    │
          └─────────┬─────────┘
                    │
                    ▼
          ┌─────────────────────┐
          │  Password Set?      │
          └─────┬───────┬───────┘
                │       │
           NO   │       │  YES
                │       │
                ▼       ▼
    ┌─────────────┐  ┌──────────────┐
    │ Set Password│  │    Login     │
    │   Screen    │  │   Screen     │
    └──────┬──────┘  └──────┬───────┘
           │                │
           └────────┬───────┘
                    │
          Successful Auth
                    │
                    ▼
          ┌─────────────────────┐
          │    Authenticated    │
          │   (Master Key Set)  │
          └─────────┬───────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
    Background   Screen Off   5 Min
     Detected    Detected    Inactivity
        │           │           │
        └───────────┴───────────┘
                    │
                    ▼
          ┌─────────────────────┐
          │    Lock App         │
          │  (Clear Keys)       │
          └─────────┬───────────┘
                    │
                    ▼
          ┌─────────────────────┐
          │   Login Required    │
          └─────────────────────┘
```

## 🗄️ Data Storage Structure

```
Keychain (Hardware-Backed)
├── Password Hash
│   └── PBKDF2(password, salt)
└── Master Key
    └── PBKDF2(password, master-salt, 100K)

MMKV (Encrypted)
├── password_set: boolean
├── password_salt: string
└── files: JSON Array
    └── [
          {
            id: string,
            originalName: string,
            encryptedPath: string,
            fileType: string,
            size: number,
            timestamp: number
          }
        ]

File System (App Sandbox)
└── secure/
    ├── .nomedia (Android)
    ├── {fileId1}.enc (encrypted file)
    ├── {fileId2}.enc (encrypted file)
    └── {fileId3}.enc (encrypted file)
```

## 🛡️ Security Layers

```
Layer 7: User Interface
         ├── Input validation
         ├── Visual feedback
         └── User actions

Layer 6: Application Logic
         ├── Authentication flow
         ├── File operations
         └── Settings management

Layer 5: Security Service
         ├── Password hashing
         ├── Key management
         ├── Encryption/Decryption
         └── Auto-lock

Layer 4: Storage Service
         ├── File operations
         ├── Metadata management
         └── Secure deletion

Layer 3: Native Modules
         ├── Keychain
         ├── Biometrics
         ├── MMKV
         └── File System

Layer 2: Operating System
         ├── Secure Enclave (iOS)
         ├── Keystore (Android)
         └── Sandboxing

Layer 1: Hardware
         ├── Secure Element
         ├── Biometric sensors
         └── Hardware encryption
```

## 📱 Component Hierarchy

```
App (_layout.tsx)
├── ThemeProvider (Dark Theme)
├── Navigation Stack
│   ├── index.tsx (Redirect)
│   ├── set-password.tsx
│   │   └── LinearGradient
│   │       ├── Header (Icon, Title, Subtitle)
│   │       ├── Form (Inputs, Button)
│   │       └── Footer (Warning)
│   │
│   ├── login.tsx
│   │   └── LinearGradient
│   │       ├── Header
│   │       ├── Form
│   │       └── Biometric Button
│   │
│   └── home.tsx
│       └── LinearGradient
│           ├── Header (Title, Settings, Lock)
│           ├── File List (FlatList)
│           │   └── File Cards
│           ├── FAB (Add Button)
│           ├── Add Menu Modal
│           ├── Settings Modal
│           └── Preview Modal
│
└── StatusBar (Light)
```

## 🔄 Lifecycle Events

```
App Start
   ↓
Check Device Security (Root/Jailbreak)
   ↓
Check Password Set
   ↓
Route to Appropriate Screen
   ↓
User Authenticates
   ↓
Generate/Load Master Key
   ↓
Start Inactivity Timer
   ↓
Listen for App State Changes
   ↓
On Background → Lock & Clear Keys
   ↓
On Foreground → Require Re-Auth
```

## 📊 Performance Considerations

```
Fast Operations:
├── MMKV Read/Write (microseconds)
├── Key Derivation (HMAC)
└── Password Hash Verification

Medium Operations:
├── Biometric Prompt
├── File Picking
└── Image Preview

Slow Operations:
├── File Encryption (depends on size)
├── File Decryption (depends on size)
└── Master Key Generation (100K iterations)

Optimizations:
├── Per-file keys (no re-encryption on password change)
├── MMKV for fast metadata access
├── In-memory decryption (no temp files)
└── Lazy loading for file list
```

---

**This architecture ensures maximum security with optimal performance!** 🔐